# vim:ft=kitty
#
# Color-valued options that must survive a theme change. Included after
# current-theme.conf so these win.

# Title bar follows the active window's background instead of GNOME's grey.
wayland_titlebar_color background
